/* Defines the stream entity */
export interface IStream {
    streamId: number;
    streamName: string;
    streamCode: string;
    releaseDate: string;
    price: number;
    description: string;
    starRating: number;
    imageUrl: string;
}

